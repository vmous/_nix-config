# Push Request Workflow

When asked to prepare a push request:

1. Make sure all packages in the workspace build successfuly
2. Identify the packages that have changes and create a commit in each of these packages
   - Use the same commit message for all packages having changes
   - Write the commit message according to known commit workflow
3. Use the same commit message to create the summary and description of the push request
